# Roadmap

## Active
- [x] Google Analytics 4 integration — gtag.js injected in SSR head from `GOOGLE_ANALYTICS_MEASUREMENT_ID` secret; SPA route-change page_view tracking added in `__root.tsx`. (Friend to finish GA property verification/setup.)

## Pending (external)
- [ ] DNS TXT record for Domain property in Google Search Console (user/DNS provider).
- [ ] GA verification & property config in Google Analytics (friend).
